import 'package:flutter/material.dart';

class CamTransferScreen extends StatelessWidget {
  const CamTransferScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CAM Transf')),
      body: const Center(child: Text('Modil CAM ap vini...')),
    );
  }
}

