import 'package:flutter/material.dart';

class MonCashScreen extends StatelessWidget {
  const MonCashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('MonCash')),
      body: const Center(child: Text('Modil MonCash ap vini...')),
    );
  }
}

