import 'package:flutter/material.dart';

class NatCashScreen extends StatelessWidget {
  const NatCashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('NatCash')),
      body: const Center(child: Text('Modil NatCash ap vini...')),
    );
  }
}

